name: 5-minute-cron
on:
  push:
    branches:
      - master
  schedule:
    - cron: "*/5 * * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Run DB cleanup script
        run: yarn run cleanup
